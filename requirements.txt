# Snowflake Query Execution Agent with SSO Authentication
# Minimal dependencies required for SSO authentication

# Snowflake connector (supports Python 3.8+)
snowflake-connector-python>=3.0.0

# Data manipulation and CSV export
pandas>=1.2.0

# Arrow format for better performance (optional but recommended)
pyarrow>=10.0.0

# Azure AD OAuth token flow (optional; use with --auth azure_ad_oauth)
msal>=1.24.0

# Streamlit dashboard (optional)
streamlit>=1.28.0
plotly>=5.18.0

# PowerPoint deck generator (build_incident_open_deck.py)
python-pptx>=0.6.21
